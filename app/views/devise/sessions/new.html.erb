<div class="mx-4 mt-4 d-flex flex-column justify-content-between">
  <h2 class="fw-medium">¡Hola, otra vez!</h2>
  <p class="">Por favor, confirma tu información para ingresar a Plan IT.</p>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field ">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "border-0 border-bottom border-dark w-100 shadow-none bg-transparent" %>
    </div>

    <div class="field my-4">
      <%= f.label :Contraseña %><br />
      <%= f.password_field :password, autocomplete: "current-password", class: "border-0 border-bottom border-dark w-100 shadow-none bg-transparent"%>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field mb-5">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end %>

    <div class="actions mb-3 d-flex justify-content-center">
      <%= f.submit "INICIAR SESIÓN", class: "text-light olaaaaa border-0 rounded b" %>
    </div>
  <% end %>
  <p class="text-center m-0">¿No tienes una cuenta aún?</p>

  <%= render "devise/shared/links" %>

  <div class="d-flex flex-column justify-content-between w-100 mt-5">
    <div class="d-flex mb-5">
      <div><%= image_tag('planit_logo.png', alt: 'PlanIt logo') %></div>
      <p class="fw-light d-none d-lg-flex fs-6">PlanIT, 2021 - All rights reserved</p>
    </div>
    <div class="d-flex gap-lg-2 justify-content-between mb-2">
      <p class="fw-light d-flex m-0">info@planit.com.uy</p>
      <p class="d-none d-lg-flex">&middot;</p>
      <p class="fw-light m-0">Tel: +598 96593615</p>
      <p class="d-none d-lg-flex">&middot;</p>
      <p class="fw-light d-none d-lg-flex fs-6">Tel: +598 97304031</p>
    </div>
    <div class="border-bottom border-dark rounded w-100 d-flex m-0"></div>
    <div class="d-flex justify-content-between mt-2">
      <p class="fw-light">PlanIT, 2021 - All rights reserved</p>
      <div class="d-flex flex-row gap-3">
        <div><%= image_tag('facebook.png', alt: 'Facebook logo') %></div>
        <div><%= image_tag('linkedin.png', alt: 'Linkedin logo') %></div>
        <div><%= image_tag('twitter.png', alt: 'Twitter logo') %></div>
        <div><%= image_tag('instagram.png', alt: 'Instagram logo') %></div>
      </div>
    </div>
  </div>
</div>
